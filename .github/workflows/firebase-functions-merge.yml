# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase (Functions & Firestore) on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: 14

      - name: Install dependencies
        run: 
          cd functions/ && npm install
          cd ../ && npm install

      - name: Deploy
        # run: npx firebase-tools deploy --token "$FIREBASE_TOKEN"
        run: firebase deploy --token $FIREBASE_TOKEN
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }} 
          # FIREBASE_SERVICE_ACCOUNT_PATH: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PATH }}' 
      
      #- uses: FirebaseExtended/action-hosting-deploy@v0
      #  with:
      #    repoToken: '${{ secrets.GITHUB_TOKEN }}'
      #    firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_KARROT_HQ }}'
      #    channelId: live
      #    projectId: karrot-hq
